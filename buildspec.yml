phases:
  install:
    commands:
      - pip install aws-sam-cli tox

  pre_build:
    commands:
      - aws configure set default.region $AWS_REGION
      - chmod 755 scripts/*.sh

  build:
    commands:
      - tox

  post_build:
    commands:
      - scripts/deploy.sh
